
/**
 * Disable SSR.
 * For most use-cases, it makes sense to initialize ScanbotSDK in a client-side rendered application
 * As such, it can be initialized on page load and used throughout the application
 */
export const ssr = false;